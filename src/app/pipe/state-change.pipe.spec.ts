import { StateChangePipe } from './state-change.pipe';

describe('StateChangePipe', () => {
  it('create an instance', () => {
    const pipe = new StateChangePipe();
    expect(pipe).toBeTruthy();
  });
});
